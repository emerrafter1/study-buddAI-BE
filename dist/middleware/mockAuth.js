"use strict";
// import { RequestHandler } from 'express';
// const authenticateUser: RequestHandler = (req, res, next) => {
//   // Get user from session/JWT (implementation depends on your auth system)
//   const user = getUserFromRequest(req); // Implement this based on your auth
//   if (!user) {
//     return res.status(401).json({ error: 'Not authenticated' });
//   }
//   req.user = { id: user.id }; // Attach to request
//   next();
// };
// export default authenticateUser
